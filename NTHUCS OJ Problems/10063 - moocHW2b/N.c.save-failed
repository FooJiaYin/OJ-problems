#include<stdio.h>

int main(void)
{
    int N, i, j;

    scanf("%d", &N);

    /* first line */
    printf("  2 -1");
    for(i=1; i<=(N-2); i++)
        printf("  0");

    /* Second line and below */
    for(i=(N-2),j=0; i>=0; i--, j++)
    {
        printf(" -1  2 -1");
    }

    return 0;
}
